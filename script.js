AOS.init({
        duration: 1000,
        once: true,
      });
function alertme()
{
        var names=document.getElementById("name").value;
        var message=document.getElementById("mes").value;
        alert("Thank you for the Response " + names);
}